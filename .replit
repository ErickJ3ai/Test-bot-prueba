modules = ["python-3.12"]
run = "pip install -r requirements.txt && python bot.py"

[nix]
channel = "stable-23_05"

[deployment]
run = ["sh", "-c", "python -m pip install -r requirements.txt && python bot.py"]

[[ports]]
localPort = 8080
externalPort = 80
